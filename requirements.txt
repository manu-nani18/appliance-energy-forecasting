# Core
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Time series
statsmodels>=0.14

# Machine learning
scikit-learn>=1.3
xgboost>=2.0

# Foundation model (Part 7): Chronos + PyTorch (CPU build works fine).
# On Windows/CPU install torch from the CPU index if the plain wheel is large:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
chronos-forecasting>=1.4

# Utilities
requests>=2.31

# Testing
pytest>=7.4
